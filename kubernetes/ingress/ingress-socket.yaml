apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: socket-ingress
spec:
  rules:
  - http:
      paths:
      - path: /socket.io
        pathType: Prefix
        backend:
          service:
            name: socket-service
            port:
              number: 80