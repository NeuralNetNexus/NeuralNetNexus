apiVersion: v1
kind: Pod
metadata:
  name: nginx
spec:
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
        - matchExpressions:
          - key: computing
            operator: In
            values:
            - yessir   
  containers:
  - name: nginx
    image: nginx
    imagePullPolicy: IfNotPresent
